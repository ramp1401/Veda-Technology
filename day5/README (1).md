# Level 1 - Day 5: Python Lists, Tuples, Sets, and Dictionaries

**Track:** Data Science  
**Task:** Task 5  
**Tools:** Python 3, Jupyter Notebook  

---

## 📌 Objective
Develop practical proficiency in creating, modifying, accessing, searching, and managing Python collection data structures (`lists`, `tuples`, `sets`, `dictionaries`) to process structured datasets.

---

## 📊 Summary Comparison Table

| Collection | Ordered | Mutable | Duplicates Allowed | Syntax | Primary Use Case in Data Science |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **List** | Yes | Yes | Yes | `[item1, item2]` | Dynamic sequences, feature lists, data pipelines |
| **Tuple** | Yes | No | Yes | `(item1, item2)` | Immutable metadata, fixed dimensions (e.g. `df.shape`), coordinates |
| **Set** | No | Yes | No | `{item1, item2}` | Deduplication, membership testing, set operations (union/intersection) |
| **Dictionary** | Yes (3.7+) | Yes | No (Keys) / Yes (Values) | `{"key": value}` | Key-value mapping, structured records, JSON/API payloads |

---

## 📂 Deliverables Included

1. **`Task_5_Python_Collections.ipynb`**:
   - Comprehensive examples of **Lists** (`append`, `remove`, `sort`, indexing, updating).
   - Immutable **Tuples** (unpacking, coordinate access, error handling demonstration).
   - **Sets** with set algebra (`union`, `intersection`, `difference`, deduplication).
   - **Dictionaries** with key lookup, `.get()`, `.pop()`, update, and `.items()` traversal.
   - Applied workflows on **Student Performance Dataset** and **Employee Dataset**.
2. **`Task_5_Python_Collections.pdf`**:
   - Polished, ready-to-submit PDF generated directly from the notebook.

---

## 🚀 How to Run

1. Open terminal in this project directory.
2. Launch Jupyter:
   ```bash
   jupyter notebook
   ```
3. Open `Task_5_Python_Collections.ipynb` and click **Kernel > Restart & Run All**.
